import { createAction, createSlice } from '@reduxjs/toolkit';

export const designerSlice = createSlice({
    name: 'designer',
    initialState: {},
    reducers: {},
});

export const {} = designerSlice.actions;

export default designerSlice.reducer;
